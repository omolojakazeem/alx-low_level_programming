#include <stdlib.h>
#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int a;
	int b;

	for (a == 0; a < 10; a++)
	{
		putchar(a);
	}
	for (b == 'A'; b <= 'F'; b++)
	{
		putchar(b);
	}
	putchar('\n');
	return (0);
}
